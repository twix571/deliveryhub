{
  "name": "ordercalc",
  "version": "1.0.0",
  "description": "Order Calculator with Live Server",
  "scripts": {
    "start": "live-server --port=8080 --open=/ --wait=500"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
